# Kafka Consumer

spring:
  application:
    name: test-consumer
  kafka:
    consumer:
      group-id:
## Due to programmatic config, no need to these!
#  kafka:
#    consumer:
#      auto-offset-reset: earliest
#      group-id: simple-messaging
